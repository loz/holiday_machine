<%= content_for :javascripts do %>
  <%= javascript_include_tag 'jquery.reveal', 'user-days' %>
<% end %>

<%= simple_form_for 'holiday_year', :remote => true do |f| %>
  <%= f.collection_select :id, HolidayYear.all, :id, :description, :selected => @selected_year.id %>
<% end %>

<div id="user_days_holder">
  <%= render :partial => "user_days_table" %>
</div>
